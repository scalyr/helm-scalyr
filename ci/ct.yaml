# Chart testing config used for local development and CI
remote: origin
target-branch: main
validate-maintainers: true
debug: true
helm-extra-args: --timeout 120s --set scalyr.k8s.clusterName="k8s-explorer-e2e-tests"
charts:
  - charts/scalyr-agent
